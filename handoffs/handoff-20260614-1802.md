# Handoff Document

## 0. Session Context (会话上下文)
- Project Root: /Users/jacklee/Projects/cccode-macbridge
- Working Directory: /Users/jacklee/Projects/cccode-macbridge (与 project root 相同)
- **本次代码改动仓库**: /Users/jacklee/Projects/cccode-ios (iOS app 仓库, HEAD `5afa010`, **未提交、未 push**)
- Read State Files: `.exec-plan/state/plan-36e824e0a9f7.json` (macbridge), `handoffs/handoff-20260614-1433.md` (上一份交接), 两个仓库的 git status/log

## 0.1 Exec-Plan Context (执行计划上下文)
- Active Plan: `docs/2026-06-13-session-loading-systemic-redesign.md`
- Canonical State File: `.exec-plan/state/plan-36e824e0a9f7.json`
- Legacy State File: none (`.claude/exec-plans/` 不存在)
- Completion Report: `docs/2026-06-13-session-loading-systemic-redesign完成情况.md` (status: missing, 未生成)
- Queue Summary: ready=0, in_progress=0, blocked=7, pending=3, done=38
- Resume Command: `/exec-plan docs/2026-06-13-session-loading-systemic-redesign.md audit`
- Notes: 7 个 blocked = phase2 manual real-device regression todos (其中至少 `phase2-ios-pagination-regression` 的 anchor stability 验收项被本次 bug 阻塞)。3 个 pending = phase3 batch-open (依赖 phase2 解除)。anchor bug 修复前,phase2 无法标 done,phase3 不可推进。另一条独立工作流 `plan-c9f27cc8e2b0.json` (MacBridge UI 规格) 不受影响。

## 1. Status Quo (当前进展)

用户报告 **anchor 稳定性回归**:codex 模式长会话滚到顶部触发向后翻页加载更早消息后,焦点**自动跳到最底部**(应停留在当前阅读位置)。这是 `phase2-ios-pagination-regression` 的 "anchor stability" 验收项暴露的 bug。

进行了 **两次修复尝试,真机复测均失败**(用户两次反馈"老样子"/"依然跳底"):

1. **native bypass**(第一次):加 `pendingOlderPagePrepend` 标记 + 贯通 `bypassBottomStick`/`preservesScrollAnchor` 到 `renderMessageWebTimeline`,让 prepend 时清掉 `pendingMessageWebScrollToBottom`,使 `.snapshotApplied` 不再 `scrollToBottom`。
2. **Web prepend lock**(第二次):在 `Timeline.tsx` 检测 `firstItemIndex` 递减(prepend)时设 1.5s 锁,锁期内强制 `followOutput=false` + `pinToStreamingBottom` 直接 return。

两条"主动滚到底"的代码路径都堵了,但真机仍跳底 → **根因不在已排除的路径,需更深入诊断**。已暂停修复。

Web 测试 35/35、Swift MessageDeduplicationTests 22/22 全通过(含 4 个新测试)。最新 build 已安装到 iPhone (container `6B5A028F`,bundle `index-BNyY-Ztd.js`)。

## 2. File Changes (文件变更)

### cccode-ios (HEAD `5afa010`, 未提交)
- `OpenCodeiOS/OpenCodeiOS/ViewModels/ChatViewModel.swift` — 加 `var pendingOlderPagePrepend = false` 属性(在 `pendingOlderMessagesAnchorID` 后)。
- `OpenCodeiOS/OpenCodeiOS/ViewModels/ChatViewModel+MessageSync.swift` — `mergeOlderMessagePage` 在 `messages=` 赋值前置 `pendingOlderPagePrepend = true`。
- `OpenCodeiOS/OpenCodeiOS/ViewModels/ChatViewModel+SessionManagement.swift` — 3 处 session 重置(resetForSessionChange / prepareForDraftSessionTransition / switchSession)各加 `pendingOlderPagePrepend = false`。
- `OpenCodeiOS/OpenCodeiOS/App/ChatUIKitContainerView.swift` — (a) `$messages` observer 检测 `pendingOlderPagePrepend` 后调 `scheduleRender(bypassBottomStick:)`; (b) `scheduleRender` 加 `bypassBottomStick` 参数,锁时清 `pendingRenderForceScroll` + 传 `preservesScrollAnchor` 给 renderTimeline; (c) `renderTimeline` + `renderMessageWebTimeline` 加 `preservesScrollAnchor` 参数,prepend 时强制 `pendingMessageWebScrollToBottom = false`; (d) **3 处 `NSLog("[ANCHOR v2] ...")` 诊断日志**(msgs-sink / render / snapshotApplied),仍保留。
- `OpenCodeiOS/OpenCodeiOSTests/MessageDeduplicationTests.swift` — 4 个新测试: `testPendingOlderPagePrepend_defaultsFalse`、`testMergeOlderMessagePage_setsPendingOlderPagePrepend`、`testMergeLatestServerPage_doesNotSetPendingOlderPagePrepend`、`testReplaceMessagesFromServer_doesNotSetPendingOlderPagePrepend`。
- `message-web/src/components/Timeline.tsx` — Web prepend lock: `prependLockedRef` + `firstItemIndex` 递减时设锁(`window.setTimeout` 1500ms 清) + `pinToStreamingBottom` 加 `prependLockedRef.current` 检查 + `followOutput={isTyping && !prependLockedRef.current ? 'auto' : false}`。
- Web bundle: `index-CRj1_S9h.js`(删)→ `index-BNyY-Ztd.js`(新); `index.html` 引用更新; `project.pbxproj` 资源引用更新。

### macbridge (无本次代码改动)
- 本次会话只在 macbridge 仓库做了诊断/查看(exec-plan state、handoffs)。14 个 `MacBridge/` 未提交文件 + `plan-c9f27cc8e2b0.json` 属于独立的 MacBridge UI 规格 workstream(`plan-c9f27cc8e2b0`),与 session loading / anchor bug 无关,**不要混入或回退**。

## 3. Known Issues (遗留问题)

1. **anchor 稳定性 bug 未修复(核心阻塞)**:翻页 prepend 后跳底。两次修复失败。
2. **已排除的路径(下次不要再试)**:
   - native `.snapshotApplied` 的 `scrollToBottom`(已 bypass,`pendingMessageWebScrollToBottom` 在 prepend 时清零)— 修复后仍跳。
   - Web `followOutput='auto'` + streaming RAF 的 `pinToStreamingBottom`(已 prepend lock)— 修复后仍跳。
   - native 其他 `scrollToBottom` 调用点(`didTapScrollToBottomButton` 仅按钮触发;`performAutomationAction` 仅自动化测试)。
   - `isGenerating`/`isTyping` 误触发:`loadOlderMessages` 开头 `guard !isGenerating`,用户能成功翻页说明 isGenerating=false → isTyping=false → followOutput 本就是 false。
3. **剩余怀疑(下次诊断方向)**:
   - **Virtuoso `firstItemIndex` 机制本身**:prepend 后视觉位置没真正保持(Virtuoso 版本行为或与 native 全量 apply snapshot 的交互)。`Timeline.tsx` line 82-93 的 firstItemIndex 计算 render-body 里跑,逻辑看似正确,但 Virtuoso 实际 scrollTop 行为未验证。
   - **native `apply(snapshot:)` 全量推送**:`MessageWebContainerView.apply` 每次 `enqueueSnapshot` + `send(.setTypingState)`,可能触发 Web React 全量重渲染,Virtuoso 内部 state 重置。
   - 某条尚未发现的 scroll 路径(native evaluateJavaScript 注入,或 Web 某处)。
4. **诊断障碍**:命令行抓真机日志不通 — `/usr/bin/log stream --device <UDID>` 在本机 macOS **不支持 `--device` 选项**(报 unrecognized option);`idevicesyslog` 抓的是旧式 syslog,**抓不到 iOS unified logging 的 NSLog**(已验证:90s 抓到 28 万行系统日志,0 条 `[ANCHOR v2]`)。NSLog 诊断代码仍留在 `ChatUIKitContainerView.swift`,但需用 **Safari Web Inspector** 或 **Mac Console.app** 手动看。
5. **7 个 phase2 回归 todo 仍 blocked**,等 anchor bug 修复后才能解除。
6. **cccode-ios 改动未提交**:两次修复的代码都在工作区,可继续迭代或回退。HEAD 仍是 `5afa010`。
7. **`build/` 目录**(cccode-ios DerivedData)untracked,勿提交。

## 4. Next Steps (下一步计划)

**第一个具体动作**:用 **Safari Web Inspector** 连真机观察 prepend 时的 scrollTop,确定 Virtuoso firstItemIndex 是否真的保持视觉位置:

1. Mac Safari → 设置 → 高级 → 勾选"显示开发者菜单功能"
2. iPhone 用数据线连 Mac,打开 CCCode → codex 长会话
3. Mac Safari → 开发菜单 → 选 iPhone → 选 CCCode 的 WebKit 页面
4. 在 Web Inspector Console 执行,翻页前后对比:
   ```js
   const s = document.querySelector('[data-testid="chat.timeline.list"]');
   console.log('scrollTop=', s.scrollTop, 'scrollHeight=', s.scrollHeight, 'clientHeight=', s.clientHeight);
   ```
5. 滚到顶触发翻页 → 再查一次 scrollTop。**如果 scrollTop 从接近 0 跳到接近 scrollHeight(底部),说明 Virtuoso/某机制在 prepend 后强制滚底**,与 firstItemIndex 应有的"保持位置"行为矛盾。

**基于诊断结果**:
- 若 scrollTop 确实跳到底 → 在 `Timeline.tsx` 的 `firstItemIndex` 递减处**前后**手动保存/恢复 `scroller.scrollTop`(绕过 Virtuoso 的 prepend 补偿失效),或检查 Virtuoso 版本是否需要 `initialTopMostItemIndex` 配合。
- 若 scrollTop 没跳但视觉跳 → 可能是 Virtuoso 重新挂载/重排,检查 `key` 或 `data` 引用稳定性。
- 若需更深日志 → 在 `Timeline.tsx` 加 `console.log`,确认 `MessageWebContainerView` 是否已配置 WKWebView `userContentController` 的 console message handler 把 Web console 传到 native(若有,可用现有 debugLog 链路;若无,加一个 `scriptMessageHandler`)。

## 5. Constraints (关键约束)
- iOS 代码在 `/Users/jacklee/Projects/cccode-ios`(NOT opencodeIosNew,后者已废弃)。
- **每个 iOS 代码改动必须 build + install 到 iPhone 才算验证**(iPhone UDID `00008140-001E69503453001C`,已配对信任)。build 命令:`DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer xcodebuild -project OpenCodeiOS/CCCode.xcodeproj -scheme CCCode -configuration Debug -destination 'platform=iOS,id=00008140-001E69503453001C' -derivedDataPath ./build/DerivedData build`;install:`xcrun devicectl device install app --device 00008140-001E69503453001C <path>/CCCode.app`。Web 改动需先 `cd message-web && npm run build:ios` 重新生成 bundle。
- **不要 push** 到任何远程(cccode-ios 和 macbridge 都是本地改动)。
- **不要跑 UI 测试/snapshot 测试/simulator 自动化**(除非用户显式授权)。
- **不要加 fallback/mock/fake-data 分支到生产路径**——暴露错误、保留失败状态。
- cccode-ios 与 macbridge 是**两个独立 git 仓库**;本次 anchor 修复全在 cccode-ios,exec-plan state 在 macbridge。
- macbridge 的 14 个 `MacBridge/` 未提交文件 + `plan-c9f27cc8e2b0.json` 属于独立 MacBridge UI workstream,**不要回退或混入** anchor 修复。
- 真机日志:`/usr/bin/log --device` 不支持;`idevicesyslog` 抓不到 NSLog(unified logging)。用 Safari Web Inspector 或 Console.app。
- simulator:iPhone 16 Pro 不存在,用 **iPhone 17 Pro** simulator(`-destination 'platform=iOS Simulator,name=iPhone 17 Pro'`)。
