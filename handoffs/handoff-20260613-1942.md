# Handoff Document

## 0. Session Context (会话上下文)
- Project Root: /Users/jacklee/Projects/cccode-macbridge
- Working Directory: /Users/jacklee/Projects/cccode-macbridge
- Read State Files: .exec-plan/state/plan-36e824e0a9f7.json, docs/2026-06-13-session-loading-baseline.md, docs/2026-06-13-session-loading-systemic-redesign.md, AGENTS.md

## 0.1 Exec-Plan Context (执行计划上下文)
- Active Plan: docs/2026-06-13-session-loading-systemic-redesign.md
- Canonical State File: .exec-plan/state/plan-36e824e0a9f7.json
- Legacy State File: none (.claude/exec-plans/ does not exist)
- Completion Report: docs/2026-06-13-session-loading-baseline.md (this is the Phase 0 baseline; the exec-plan auto completion report docs/2026-06-13-2026-06-13-session-loading-systemic-redesign完成情况.md is still missing, queue not fully complete)
- Queue Summary: ready=0, in_progress=1 (phase2-transcript-index-impl), blocked=0, done=15, total=27
- Resume Command: /exec-plan docs/2026-06-13-session-loading-systemic-redesign.md start
- Notes: The in_progress item phase2-transcript-index-impl has both dependencies (phase1-claude-global-index-regression, phase1-codex-cache-hardening-regression) done, so it is ready to continue. The queue has no blocked items. Worktree has unrelated pre-existing uncommitted changes in MacBridge/ (AppDependencies, ViewModels, Views, project.yml, OpenCodeCredentialsGenerator.swift, MacBridgeBehaviorTests.swift) that are NOT part of this plan and must be left alone.

## 1. Status Quo (当前进展)
Phase 0 (测量) and Phase 1 (Claude 全局索引 + Codex 缓存加固) are fully done and proven. Phase 2 transcript page index implementation is marked in_progress but no Phase 2 code has been written yet. The real-dataset baseline (124 requests, 0 errors, cold/hot x5 over direct+relay) is captured and the baseline report is written. Phase 0 device regression confirmed the production failure mode: large Codex history produces a ~12.45MB single WebSocket frame that iOS rejects with close 1009 "Message too long", causing the session-open timeout/blank-screen; it is NOT a crash or jetsam. Phase 1 Claude global index reduced hot list latency from ~116-125ms to ~0.77-1.14ms (cache_hit=true, zero re-parse on hot).

## 2. File Changes (文件变更)
This-session plan changes (untracked, not yet committed):
- go-bridge/claude_session_catalog.go (NEW): cross-project Claude session index with mtime+size fingerprint, copy-on-write snapshot, singleflight refresh, directory filtering, snapshot-filter path wired into handleListSessions.
- go-bridge/claude_session_catalog_test.go (NEW): covers first build, zero-change hit, single-file refresh, same-mtime/different-size, deletion, cross-project collision isolation, concurrent non-blocking readers, filter reuse.
- agent/codex/list.go (MODIFIED): fingerprint now mtime+size; sorted tie-breaker fixed to ModifiedAt DESC, SessionID ASC.
- agent/codex/list_cache_test.go (MODIFIED): added size-fingerprint and deterministic-ordering tests.
- core/session_load_metrics.go + _test.go (NEW): request-scoped metrics (enumerate/stat_compare/metadata_parse/history_parse, cache counters, dataset/max-file bytes) propagated via context.
- go-bridge/session_load_metrics.go + _test.go (NEW): per-request transport route (direct/relay), wire mapping/json encode/socket send timing, response byte accounting, unified trace log including request_id.
- go-bridge/handlers.go (MODIFIED): handleListSessions and handleGetSessionMessages now build and emit session loading metrics; Claude list path uses the new catalog when available.
- go-bridge/handlers_test.go (MODIFIED): assertions updated for catalog-backed Claude path.
- go-bridge/session_loading_baseline_test.go (NEW, env-gated CCCODE_SESSION_LOADING_BASELINE=1): reproducible real-dataset matrix over direct+relay, cold+hot x5, plus dataset descriptors; skipped by default.
- docs/2026-06-13-session-loading-baseline.md (NEW): Phase 0 baseline report with tables, go/no-go decisions, close-1009 root cause.
- docs/2026-06-13-session-loading-review*.md and docs/2026-06-13-session-loading-systemic-redesign.md (NEW): review reports and the approved design doc.

Pre-existing untracked/modified files NOT touched by this plan (do not revert): MacBridge/* (project.pbxproj, AppDependencies.swift, MacBridgeApp.swift, Localization.swift, ManagementAPIClient.swift, *ViewModel.swift, *View.swift, project.yml, OpenCodeCredentialsGenerator.swift, Components/, MacBridgeBehaviorTests.swift).

⚠️ **iOS 仓库校准（2026-06-13，覆盖下方原始记录）**：本节最初写的是拆仓前的 `/Users/jacklee/Projects/opencodeIosNew`（`UnifiedBridge*` 架构），该仓库已于 2026-06-01 拆仓（审核 `proved-complete`）后被 `/Users/jacklee/Projects/cccode-ios`（`CCCodeBridge*` 架构）取代并废弃。因此：Phase 0 的 iOS 埋点（`SessionLoadingTracer`）只存在于死仓库 `opencodeIosNew`，**正牌仓库 `cccode-ios` 不含该埋点**（其 `Utilities/PerformanceTracer.swift` 存在但无 session-loading 测量）。后续 iOS 工作必须在 `cccode-ios` 进行，埋点需在新仓库补建。close-1009 根因与 iOS 仓库无关，Phase 0/1 + Phase 2 后端工作不受影响。

iOS repo (separate worktree at /Users/jacklee/Projects/opencodeIosNew — **SUPERSEDED**, see calibration note above) already has instrumentation applied (SessionLoadingTracer in PerformanceTracer.swift, UnifiedBridgeTransport/Adapter, ChatViewModel+MessageSync) and was installed to the connected iPhone 16 Pro. No iOS Phase 2 changes have been made yet. **Update 2026-06-13: iOS Phase 2 Stage 1 (close-1009 client fix) has since been implemented in the active repo `/Users/jacklee/Projects/cccode-ios` — capability-gated `paginate:true` first-page fetch; iOS simulator build SUCCEEDED. See exec-plan `phase2-ios-pagination-impl` notes.**

Artifacts under .exec-plan/artifacts/session-loading/phase0/ and phase1-* capture raw Bridge logs, real-dataset metrics JSONL/TSV/summary, iOS device console logs, Codex fingerprint snapshots, and test logs.

## 3. Known Issues (遗留问题)
- Large Codex session (019d1a4c, 47.8MB, 51 logical messages -> 12.45MB response) triggers iOS WebSocket close 1009 "Message too long" on direct LAN; this is the root cause of the user-reported open timeout/blank screen. Phase 2 cursor pagination is the fix (do NOT just raise the frame limit as a workaround).
- Codex hot list still re-parses 2-3 rollout files per request because Codex actively appends to live sessions; acceptable per baseline but worth revisiting if title freshness is revisited.
- Baseline report lists performance targets but they are provisional until Phase 2 lands.
- Auto completion report (docs/2026-06-13-2026-06-13-session-loading-systemic-redesign完成情况.md) is intentionally NOT generated yet because the queue is not fully complete.

## 4. Next Steps (下一步计划)
Begin phase2-transcript-index-impl: implement the boundary-safe TranscriptPageIndex per design Sections 6.1-6.4. Start by implementing the per-session logical-message span model (ReplayStart/EndOffset/OpenToolUses), the Codex tool-output closure variants (function_call_output, custom_tool_call_output, event_msg/patch_apply_end), and the tail-replay + interval-union page rebuild in a new Go package, then wire it into get_session_messages behind the session-pagination capability. Keep the existing full-parse path as the compatibility fallback. After impl, add the -tests and -regression triplets. Resume via: /exec-plan docs/2026-06-13-session-loading-systemic-redesign.md start

## 5. Constraints (关键约束)
- Do NOT add fallback, mock data, placeholder, or fake-success paths to mask the close-1009 failure; Phase 2 pagination must solve it for real.
- Do NOT run UI tests, snapshot tests, or simulator automation without explicit user approval; prefer code reading, targeted build, targeted unit test.
- After iOS code changes, install to the connected iPhone when one is available (iPhone 16 Pro, id BFC431AC-C205-56B2-BB4D-9EC0C57A0C05).
- Cursor pagination must preserve logical message boundaries (assistant/reasoning/tool_use/tool_result groupings); never reverse-truncate by raw line or byte offset.
- Codex cursor key is (modifiedAtMillis DESC, sessionID ASC); Claude cursor key is (updatedAtMillis DESC, projectKey ASC, sessionID ASC). Never encode timestamp alone.
- Preserve unrelated uncommitted changes in MacBridge/; they are not part of this plan.
- Do not modify OpenCode upstream API; ocProxy capabilities are assessed separately.
- Bridge protocol major version must not change; new fields/methods are additive and backwards compatible.
