# Handoff Document

## 0. Session Context (会话上下文)
- Project Root: /Users/jacklee/Projects/cccode-macbridge
- Working Directory: /Users/jacklee/Projects/cccode-macbridge (same)
- Read State Files: .exec-plan/state/plan-36e824e0a9f7.json, .exec-plan/artifacts/session-loading/, git log for both repos

## 0.1 Exec-Plan Context (执行计划上下文)
- Active Plan: docs/2026-06-13-session-loading-systemic-redesign.md
- Canonical State File: .exec-plan/state/plan-36e824e0a9f7.json
- Legacy State File: none (.claude/exec-plans/ does not exist)
- Completion Report: docs/2026-06-13-session-loading-systemic-redesign完成情况.md (status: missing, not yet generated)
- Queue Summary: ready=3, in_progress=0, blocked=7, done=38
- Resume Command: `/exec-plan docs/2026-06-13-session-loading-systemic-redesign.md start`
- Notes: 7 blocked items are all phase2 regression todos that require manual real-device verification of backward pagination on iPhone 16 Pro. The 3 ready items are phase3-batch-open-decision-impl/tests/regression, which depend on phase2-ios-pagination-regression (currently blocked).

## 1. Status Quo (当前进展)
Two local commits landed on `main` in each repo (NOT pushed to remote):

- **cccode-ios** `main @ 5afa010` — `fix: preserve paginated session history` (18 files, +767/-60)
- **cccode-macbridge** `main @ ec674f0` — `feat: add bounded session history pagination` (64 files, +12033/-146)

The session history pagination subsystem is code-complete and committed: backend cursor-based pagination, transcript-index extractor with span boundary fixes, stable synthetic message IDs, iOS backward pagination via Virtuoso atTop/startReached triggers, and the paginated-probe fix that prevents the Codex external turn probe from overwriting full paginated history with the latest 2-message slice. All 18 focused unit tests pass on both Go and Swift sides.

Work stopped at the point where the latest fix build was installed and launched on iPhone 16 Pro (session 019ebbd4-a7dc-73f2-aa40-69aa992d2e0d), but the user's most recent manual retest still showed only the last conversation turn. The paginated-probe regression is still blocked pending a fresh retest against the latest installed build.

## 2. File Changes (文件变更)

### cccode-ios (commit 5afa010)
- `ChatViewModel+Generation.swift` — Codex external turn probe now compares against local same-length tail signature and calls `mergeLatestServerPage` instead of `replaceMessagesFromServer`, preventing full-history overwrite by the latest 2-message probe result.
- `ChatViewModel+MessageSync.swift` — Added `loadOlderMessages`, `updateHistoryPaginationState`, `mergeOlderMessagePage`, `mergeLatestServerPage` methods. First-page load now triggers `updateHistoryPaginationState` when the backend supports pagination. Bridge network-interruption guard added for `loadMessages`.
- `ChatViewModel+SessionManagement.swift` — Pagination state reset on session switch.
- `ChatViewModel.swift` — Added `historyPaginationState`, `hasPendingOlderMessagesRequest`, `pendingOlderMessagesAnchorID`, `isLoadingOlderMessages` properties.
- `ChatUIKitContainerView.swift` — Removed premature `isLoadingMessages` guard so MessageWeb `loadOlder` events reach ChatViewModel during initial load.
- `Timeline.tsx` — Virtuoso `atTopStateChange(true)` now routes through the same cooled `loadOlder` trigger as `startReached`.
- `BackendClient.swift`, `BackendModels.swift`, `BridgeMessageMapping.swift`, `CCCodeBridgeBackendClient.swift`, `CCCodeBridgeClient.swift`, `CCCodeBridgeTransport.swift` — Added `SessionHistoryPaginating` protocol, `BackendFetchMessagesResult` with cursor fields, and Bridge pagination request/response wire mapping.
- `MessageDeduplicationTests.swift` — Added pagination state dedup, cursor chain, older-page merge, and latest-page merge tests.
- `CCCodeBridgePhase3Tests.swift`, `BridgeAdapterTests.swift` — Added pagination transport and adapter tests.
- `App.test.tsx` — Added atTopStateChange pagination trigger test (34/34 pass).
- Embedded Web bundle: `index-BTpYeZI6.js` → `index-CRj1_S9h.js`.

### cccode-macbridge (commit ec674f0)
- `go-bridge/pagination.go` (new, 387 lines) — Cursor-based backward pagination with 1MB byte budget, span boundary, and stable synthetic IDs.
- `go-bridge/handlers.go` — `get_session_messages` handler now supports `beforeCursor` pagination; advertises `session_pagination` capability.
- `transcriptindex/` (new package, 15 files) — Transcript-index store with extractor, lineage, anchor, cursor, persistence, and span boundary regression tests.
- `core/session_load_metrics.go` (new) — Request-scoped session loading metrics propagated through context.
- `agent/codex/rich_history.go`, `agent/codex/list.go` — Codex history parsing and list cache instrumentation.
- `go-bridge/session_load_metrics.go` — Handler-level wire mapping, JSON encoding, response byte, route logging.
- `relay-server/` — `MaxFrameBytes` default raised from 512KB to 2MB to accommodate encrypted pagination envelopes.
- `core/interfaces.go` — Added `TranscriptLocator` interface.
- `go-bridge/claude_session_catalog.go` (new) — Claude session catalog for pagination support.
- Protocol docs: `bridge-v1.md`, `bridge-v1.types.ts`, and 4 sample JSON files for paginated request/response.
- Design docs: 4 review docs + baseline + systemic redesign.
- `.exec-plan/state/plan-36e824e0a9f7.json` — Full exec-plan state with 48 todos.
- `.exec-plan/artifacts/session-loading/` — Phase 0/1/2 evidence artifacts (metrics, logs, fingerprint snapshots).

## 3. Known Issues (遗留问题)
1. **7 blocked regression todos** — All phase2 manual real-device verifications remain blocked. The latest installed iOS build (session 019ebbd4-a7dc-73f2-aa40-69aa992d2e0d) needs a fresh retest: open the session, scroll to top, confirm earlier turns prepend and the probe no longer overwrites history.
2. **Phase 3 batch-open decision** — 3 ready todos are dependency-gated by `phase2-ios-pagination-regression` (blocked). Evidence already points to NO-GO (no extra serial RPC above 150ms threshold).
3. **`build/` directory in cccode-ios** — Untracked, intentionally not committed (DerivedData).
4. **Independent Mac UI changes** — `MacBridge/` SwiftUI/localization/pairing changes (14 files) are uncommitted in cccode-macbridge worktree. They are unrelated to session loading and were intentionally excluded from the pagination commit.
5. **Neither commit pushed to remote** — `origin/main` is behind by 1 commit on each repo.
6. **Persistence cache invalidation** — The transcript-index fingerprint does not include extractor logic version, so a logic change won't invalidate a stale cached index. Long-term design fix needed.

## 4. Next Steps (下一步计划)
Immediate: retest session 019ebbd4 on the latest installed iPhone build to unblock the 7 phase2 regression todos. If pagination works correctly, mark regressions done and run `/exec-plan docs/2026-06-13-session-loading-systemic-redesign.md start` to process the phase3 batch-open decision (expected NO-GO with evidence).

If the retest still fails, check Bridge logs at `/tmp/go-bridge.log` for `hasBeforeCursor=true` requests — their absence means the atTop trigger is still not firing on the device.

## 5. Constraints (关键约束)
- iOS code lives in `/Users/jacklee/Projects/cccode-ios` (NOT opencodeIosNew, which is deprecated).
- Do NOT push to GitHub without explicit user permission — these are local commits only.
- Do NOT run UI tests, snapshot tests, or simulator automation without explicit user confirmation.
- Do NOT add fallback/mock/fake-data branches to production paths — expose errors and preserve failure states.
- Every iOS code change must be installed to the connected iPhone before considering the step done.
- The independent `MacBridge/` UI changes in the worktree were not part of this commit and must not be reverted or mixed in.
